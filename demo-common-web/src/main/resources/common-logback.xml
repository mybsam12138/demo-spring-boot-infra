<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- ========================= -->
    <!-- 1. Spring properties -->
    <!-- ========================= -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="demo-app"/>

    <springProperty scope="context" name="ACTIVE_PROFILE" source="spring.profiles.active" defaultValue="default"/>

    <!-- ========================= -->
    <!-- 2. Log pattern -->
    <!-- ========================= -->
    <property name="CONSOLE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %X{traceId} %logger{36} - %msg%n"/>

    <!-- API log pattern (concise, one-line) -->
    <property name="API_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %X{traceId} %msg%n"/>

    <!-- ========================= -->
    <!-- Log path (portable) -->
    <!-- ========================= -->
    <property name="LOG_PATH" value="${LOG_PATH:-logs}"/>

    <!-- ========================= -->
    <!-- 3. Appenders -->
    <!-- ========================= -->

    <!-- Console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- File -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">

        <!-- active log file -->
        <file>${LOG_PATH}/app.log</file>

        <!-- daily rolling -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ========================= -->
    <!-- API log (separated) -->
    <!-- ========================= -->

    <appender name="API_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">

        <!-- active api log file -->
        <file>${LOG_PATH}/api.log</file>

        <!-- daily rolling, short retention -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/api.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>

        <encoder>
            <!-- API logs should be concise -->
            <pattern>
                ${API_LOG_PATTERN}
            </pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>


    <!-- ========================= -->
    <!-- 3.1 Async wrappers -->
    <!-- ========================= -->

    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>8192</queueSize>
        <discardingThreshold>20</discardingThreshold>
        <neverBlock>true</neverBlock>
        <appender-ref ref="FILE"/>
    </appender>

    <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>2048</queueSize>
        <discardingThreshold>20</discardingThreshold>
        <neverBlock>true</neverBlock>
        <appender-ref ref="CONSOLE"/>
    </appender>

    <appender name="ASYNC_API_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>4096</queueSize>
        <discardingThreshold>20</discardingThreshold>
        <neverBlock>true</neverBlock>
        <appender-ref ref="API_FILE"/>
    </appender>

    <!-- ========================= -->
    <!-- 4. Root logger -->
    <!-- ========================= -->
    <root level="INFO">
        <appender-ref ref="ASYNC_CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </root>

    <!-- ========================= -->
    <!-- 5. Framework noise control -->
    <!-- ========================= -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.apache.catalina" level="WARN"/>

    <!-- ========================= -->
    <!-- API log logger -->
    <!-- ========================= -->
    <logger name="API_LOG" level="INFO" additivity="false">
        <appender-ref ref="ASYNC_API_FILE"/>
        <appender-ref ref="ASYNC_CONSOLE"/>
    </logger>

    <!-- ========================= -->
    <!-- 6. Your application -->
    <!-- ========================= -->
    <logger name="com.github.mybsam12138" level="INFO" additivity="false">
        <appender-ref ref="ASYNC_CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

</configuration>
